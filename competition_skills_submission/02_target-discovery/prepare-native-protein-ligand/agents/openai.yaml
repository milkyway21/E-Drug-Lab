interface:
  display_name: Prepare Native Protein Ligand
  short_description: Prepare clean receptor PDB and native ligand SDF
  default_prompt: Download the selected RCSB complex, clean the target chains, extract the correct native ligand without moving it, validate coordinates, and declare the strict DiffDynamic clean-PDB/native-SDF handoff before pocket qualification.
